# Python environment
python>=3.10

# Core Python packages
pandas>=2.2.3
numpy>=2.1.1
matplotlib>=3.8.0
ete3>=3.1.3
duckdb>=1.1.3
pysam>=0.21.0
pyarrow>=15.0.0 # for pandas parquet support

# Notes:
# - MMseqs2 must be available either via Singularity image or native installation.
#   It is NOT installable via pip. Ensure it's in your PATH or container.
# - Built-in Python libraries (os, json, csv, argparse, subprocess, collections, pickle, time)
#   are not listed here since they are included with Python.

